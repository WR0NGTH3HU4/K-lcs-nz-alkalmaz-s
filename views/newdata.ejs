<%- include('header') %>


<%- include('message') %>
<div class="newdata">
     <form action="/rentals/newdata" method="post">
        <div class="row gy-3 overflow-hidden">
            <div class="col-12">
                <div class="form-floating mb-3">
                    <input type="text" class="form-control" name="title" id="title" placeholder="">
                    <label for="email">Cím</label>
                </div>
            </div>
            <div class="col-12">
                <div class="form-floating mb-3">
                    <!-- name="passwd" helyett name="password" -->
                    <input type="text" class="form-control" name="type" id="type" placeholder="">
                    <label for="passwd">Típus</label>
                </div>
            </div>
            <div class="col-12">
                <div class="d-grid">
                    <input class="btn btn-primary mb-3 sendAuth" type="submit" value="Hozzáad">
                </div>
            </div>
        </div>
    </form>
</div>

<%- include('footer') %>